Branch is separate changes from main master. It's like parallel universe with the same files at initial creating of a branch, and after it goes it's own way. Branch can be merged with master if necessary.
